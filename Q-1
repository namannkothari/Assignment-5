#include <stdio.h>

void main()
{
    int n,i,sum=0;
    printf("Enter the number upto which you want to sum:");
    scanf("%d",&n);
    for(i=0;i<=n;i++)
    {
        sum=sum+i;
    }
    printf("sum is: %d",sum);
}

